<!-- res/drawable/border_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#288a8d99" /> <!-- Shadow color -->
            <corners android:radius="8dp" /> <!-- Corner radius in dp -->
        </shape>
    </item>

    <!-- Border and background layer -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimaryVariant" /> <!-- Background color -->
            <corners android:radius="8dp" /> <!-- Corner radius in dp -->
            <stroke
                android:width="1dp"
                android:color="?attr/colorPrimaryVariant" /> <!-- Border color -->
        </shape>
    </item>
</layer-list>
